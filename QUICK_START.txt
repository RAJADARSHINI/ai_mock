╔════════════════════════════════════════════════════════════════════════╗
║                 AI MOCK INTERVIEW - QUICK START GUIDE                  ║
║                                                                        ║
║  ⚠️  CRITICAL: DO NOT USE VS CODE SIMPLE BROWSER!                     ║
║     Camera & Microphone are BLOCKED in VS Code's internal browser.   ║
║     You MUST use Chrome, Edge, or Firefox (external application).    ║
╚════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────┐
│ STEP 1: START BACKEND                                                 │
└────────────────────────────────────────────────────────────────────────┘

  Choose ONE backend to run:

  ┌─ PYTHON BACKEND (Flask) ─────────────────────────────────┐
  │  1. Open Terminal in VS Code                             │
  │  2. Run: cd python_backend                               │
  │  3. Run: python app.py                                   │
  │  4. Keep terminal open                                   │
  │  5. You'll see: http://localhost:5000                    │
  └──────────────────────────────────────────────────────────┘

  ┌─ NODE BACKEND (Express) ─────────────────────────────────┐
  │  1. Open Terminal in VS Code                             │
  │  2. Run: cd backend                                      │
  │  3. Run: npm install (first time only)                   │
  │  4. Run: npm start                                       │
  │  5. Keep terminal open                                   │
  │  6. You'll see: http://localhost:5000                    │
  └──────────────────────────────────────────────────────────┘

  ┌─ OR USE BATCH FILES ─────────────────────────────────────┐
  │  • Double-click: START_PYTHON_BACKEND.bat                │
  │  • Double-click: START_NODE_BACKEND.bat                  │
  └──────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────┐
│ STEP 2: START FRONTEND SERVER (if needed)                             │
└────────────────────────────────────────────────────────────────────────┘

  If your HTML files need a web server:

  1. Open NEW terminal in VS Code
  2. Run: python -m http.server 8000
  3. Keep this terminal open too
  4. You'll see: Serving HTTP on :: port 8000

  OR double-click: START_FRONTEND.bat

┌────────────────────────────────────────────────────────────────────────┐
│ STEP 3: OPEN IN EXTERNAL BROWSER ⚠️  CRITICAL!                        │
└────────────────────────────────────────────────────────────────────────┘

  1. *** OPEN CHROME OR EDGE (external application) ***
     NOT the VS Code Simple Browser!

  2. Navigate to: http://localhost:8000

  3. When prompted: ALLOW camera and microphone permissions

  4. Your app should now work with full camera/mic access! ✅

┌────────────────────────────────────────────────────────────────────────┐
│ VERIFICATION CHECKLIST ✅                                              │
└────────────────────────────────────────────────────────────────────────┘

  □ Backend terminal is running
  □ Terminal shows: "Backend URL: http://localhost:5000"
  □ Chrome or Edge is open (NOT VS Code)
  □ URL bar shows: http://localhost:8000 (NOT file:///)
  □ Camera/microphone permissions granted in browser
  □ No CORS errors in browser console (F12)

┌────────────────────────────────────────────────────────────────────────┐
│ WHAT WAS CONFIGURED FOR YOU ✅                                         │
└────────────────────────────────────────────────────────────────────────┘

  ✅ VS Code settings updated to prevent auto-opening Simple Browser
  ✅ Python backend configured with proper CORS (allows all origins)
  ✅ Node backend configured with proper CORS (allows all origins)
  ✅ Backend URLs clearly printed in terminal output
  ✅ Flask debug mode disabled (prevents auto-browser opening)
  ✅ Clear instructions added to backend startup messages

┌────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING 🔧                                                     │
└────────────────────────────────────────────────────────────────────────┘

  ❌ Camera/microphone blocked?
     → You're in VS Code Simple Browser
     → Solution: Close it, use Chrome/Edge instead

  ❌ "CORS error" in browser console?
     → Backend not running
     → Solution: Check terminal, restart backend

  ❌ "Connection refused"?
     → Backend not started or port 5000 in use
     → Solution: Start backend, check terminal output

  ❌ Frontend not loading?
     → Using file:/// instead of http://
     → Solution: Start frontend server (python -m http.server 8000)

┌────────────────────────────────────────────────────────────────────────┐
│ IMPORTANT FILES & DOCUMENTATION 📚                                     │
└────────────────────────────────────────────────────────────────────────┘

  📄 SETUP_INSTRUCTIONS.md          Comprehensive setup guide
  📄 START_BACKEND_INSTRUCTIONS.txt Quick reference (this file)
  📄 python_backend/README_SETUP.md Python backend details
  📄 backend/README_SETUP.md        Node backend details

  🚀 START_PYTHON_BACKEND.bat       Launch Python backend
  🚀 START_NODE_BACKEND.bat         Launch Node backend
  🚀 START_FRONTEND.bat             Launch frontend server

┌────────────────────────────────────────────────────────────────────────┐
│ REMEMBER THE GOLDEN RULE! 🌟                                          │
└────────────────────────────────────────────────────────────────────────┘

  Backend running in TERMINAL
          +
  Frontend opened in CHROME/EDGE (external browser)
          =
  Camera & Microphone WORK! 🎉

╔════════════════════════════════════════════════════════════════════════╗
║  Need help? Check SETUP_INSTRUCTIONS.md for detailed information.     ║
╚════════════════════════════════════════════════════════════════════════╝
